<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Employee</title>   
</head>
<body>
    <% if (employee) { 
        for(var i=0; i < employee.length; i++){ %>
    <form method="post" action="/admin/update/<%= employee[i].id %>">
    <fieldset>
        
				<table>

					<tr>
						<td>Name</td>
						<td><input type="text" name="name" value="<%= employee[i].name %>"></td>
                    </tr>
                    <tr>
						<td>Phone</td>
						<td><input type="text" name="phone" value="<%= employee[i].phone %>"></td>
                    </tr>
                    <tr>
						<td>Username</td>
						<td><input type="text" name="username" value="<%= employee[i].username %>"></td>
                    </tr>
                    <tr>
						<td>Password</td>
						<td><input type="password" name="password" value="<%= employee[i].password %>"></td>
					</tr>
					<tr>
						<td><input type="submit" name="submit" value="Submit"></td>
					</tr>
				</table>
    </fieldset>
</form>
<% }} %>
</body>
</html>